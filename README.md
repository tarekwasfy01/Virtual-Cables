# Virtual-Cables
Virtual Cables takes a different approach from traditional virtual audio cables. Instead of using a custom audio driver, it creates up to 32 virtual USB audio devices through USB/IP. Windows connects them using its built-in USB audio driver. Each cable links a playback endpoint directly to a recording endpoint.
